app:
  db:
    url: jdbc:h2:mem:companies
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  admin:
    # Shared secret for X-Admin-Key on /api/admin/* (see README Authentication).
    api-key: local-admin-key-change-me
  ssl:
    keystore-path: classpath:ssl/keystore.p12
    keystore-password: changeit
